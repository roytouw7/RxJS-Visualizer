The unit tests use the test-helper class.
This class assists unit test by loading pairs of input source files with expected output files in json format.
These pairs of files can be found in the test-assets directory.